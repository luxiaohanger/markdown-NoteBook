# 并发编程感悟

1.线程的分工边界很重要，哪些变量、组件会被多个线程接触，需要处理竞态条件

2.线程间异步通信问题：主线程按照栈函数顺序依次执行，除了 wait、join 等显式阻塞，需要设计线程间通信方式，在目的语义下进行。

例如主线程是否等待任务队列全部完成，关系到退出任务闭包可能先于普通任务闭包进入任务队列。

3.闭包传递的生命周期问题：闭包函数传递给任务线程执行，执行时传递的数据是否可以访问？有没有可能原对象已经析构？